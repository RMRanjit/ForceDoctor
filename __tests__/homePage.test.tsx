//Right now, we are facing issue with Rendering Home. Need to fix it.
import { render, screen } from '@testing-library/react';
import Home from '@/app/page';

describe("Home", () => {
  it("renders a heading", () => {
    console.log("Started Jest Unit Testing");
    // render(<Home />);

  });
});